name: iOS CI - FlyFunBrief

on:
  push:
    branches: [main, master, ios-ci]
    paths:
      - 'app/FlyFunBrief/**'
      - 'app/FlyFunBrief.xcodeproj/**'
      - '.github/workflows/ios.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'app/FlyFunBrief/**'
      - 'app/FlyFunBrief.xcodeproj/**'
  workflow_dispatch:

jobs:
  build:
    name: Build FlyFunBrief
    runs-on: macos-latest

    steps:
    - name: Checkout flyfun-apps
      uses: actions/checkout@v4

    - name: Clone rzflight dependency to correct path
      run: |
        git clone --depth 1 https://github.com/roznet/rzflight.git /Users/runner/work/rzflight

    - name: Select Xcode version
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable

    - name: Show Xcode version
      run: xcodebuild -version

    - name: Build FlyFunBrief
      working-directory: app
      run: |
        xcodebuild build \
          -project FlyFunBrief.xcodeproj \
          -scheme FlyFunBrief \
          -destination 'platform=iOS Simulator,name=iPhone 16' \
          -configuration Debug \
          CODE_SIGNING_ALLOWED=NO \
          ONLY_ACTIVE_ARCH=YES

  # Tests are optional - uncomment to enable
  # test:
  #   name: Test FlyFunBrief
  #   runs-on: macos-latest
  #   needs: build
  #   steps:
  #   - uses: actions/checkout@v4
  #   - run: git clone --depth 1 https://github.com/roznet/rzflight.git /Users/runner/work/rzflight
  #   - uses: maxim-lobanov/setup-xcode@v1
  #     with:
  #       xcode-version: latest-stable
  #   - name: Run Unit Tests Only (skip UI tests)
  #     working-directory: app
  #     run: |
  #       xcodebuild test \
  #         -project FlyFunBrief.xcodeproj \
  #         -scheme FlyFunBrief \
  #         -destination 'platform=iOS Simulator,name=iPhone 16' \
  #         -only-testing:FlyFunBriefTests \
  #         CODE_SIGNING_ALLOWED=NO
