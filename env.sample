# FlyFun Apps configuration

# environment run as production or development
ENVIRONMENT=development

# Database location
#    airports.db and rules.json are provided with flyfun-apps, ga_persona.db, rules_vector_db are built from tools
WORKING_DIR=/path/to/flyfun-apps
AIRPORTS_DB=${WORKING_DIR}/data/airports.db
RULES_JSON=${WORKING_DIR}/data/rules.json
GA_NOTIFICATIONS_DB=${WORKING_DIR}/data/ga_notifications.db

# Vector database configuration
# For local development, use VECTOR_DB_PATH (file system)
VECTOR_DB_PATH=${WORKING_DIR}/out/rules_vector_db
# For production with ChromaDB service, use VECTOR_DB_URL (takes precedence over VECTOR_DB_PATH)
# VECTOR_DB_URL=http://chromadb:8000
# Optional: ChromaDB authentication token (if using authenticated service)
# CHROMADB_AUTH_TOKEN=your-auth-token

GA_PERSONA_DB=${WORKING_DIR}/data/ga_persona.db

# Logging
LOG_LEVEL=INFO

# Aviation Agent
# Feature flag to enable/disable the aviation agent
AVIATION_AGENT_ENABLED=1

# Configuration file selection (NEW - preferred method)
# Set to the name of a config file in configs/aviation_agent/ (without .json extension)
# Defaults to "default" if not set. See designs/AVIATION_AGENT_CONFIGURATION_ANALYSIS.md
# AVIATION_AGENT_CONFIG=default

# Legacy LLM model overrides (DEPRECATED - prefer config file)
# These still work but should be configured in configs/aviation_agent/{config_name}.json instead
# AVIATION_AGENT_PLANNER_MODEL=gpt-4o-mini
# AVIATION_AGENT_FORMATTER_MODEL=gpt-4o-mini
# ROUTER_MODEL=gpt-4o-mini

MCP_BASE_URL=https://mcp.your-domain.net/euro_aip

# Keys
OPENAI_API_KEY=your-openai-key
# COHERE_API_KEY=your-cohere-key  # Required for Cohere reranking (if using cohere provider in config)
GEOAPIFY_API_KEY=your-geoapify_key

LANGCHAIN_API_KEY=your-langsmith-key
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=YourLangsmithProjectName
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
